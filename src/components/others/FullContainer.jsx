import React from "react";

const FullContainer = ({ children }) => {
    return <section className="flex h-auto w-full justify-center py-6 lg:py-12">{children}</section>;
};

export default FullContainer;
